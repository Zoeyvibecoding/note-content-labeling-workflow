---
name: note-content-labeling-workflow
description: 从小红书投放底表筛选指定品牌或产品的消耗 Top N 图文与视频笔记，通过 Athena 获取真实笔记内容，分别执行完整视频理解和全部图片理解，再调用嵌入式美护打标引擎逐步打标，最终交付含视频、图文双 Sheet 的内容理解与打标过程文件。用于品牌与竞品内容打标、Top N 笔记研究、重新打标、字典规则更新后的全量重判，以及投放数据与内容事实 mapping。
---

# 笔记内容打标工作流

严格分离四层：投放数据、内容获取、内容理解、标签判断。后链路表现不得反向改变内容事实或标签。

## 首次响应

收到打标请求后，先向用户发送下面的提示，不要立即抓取：

> 请提供：
> 1. 从 [投放运营实操复盘](https://insight.devops.xiaohongshu.com/self-service?resourceId=6959712&shortcutId=17900802) 下载的 Excel 底表；
> 2. 每个分析对象的名称、角色（本品/竞品）、SPU name，以及 nick name/常用别名；
> 3. 视频和图文各需要分析“消耗 Top 几”（可分别设置）。
>
> 默认使用底表全部时间范围，不额外按发布时间筛选；如需限定时间，请明确提出。
>
> 粗略成本：每 10 条视频约 30–60 分钟、4万–8万 tokens；每 10 条图文约 15–30 分钟、2万–5万 tokens。实际受视频时长、图片数、Athena 网络和 OCR/ASR 质量影响。建议先做每类 Top20–50 验证口径，再决定是否扩至 Top100 或全量。

用户不需要提供标签字典网址。运行时规则由嵌入式打标引擎自动获取并锁定。

## 输入门槛

开始前确认：

- Excel 文件可读，含笔记 ID、笔记类型、SPU 或产品标识、竞价运营消耗及需要保留的投放指标。
- 每个分析对象都有唯一角色、SPU name 和至少一个 nick name；别名用于召回，不替代笔记 ID。
- 视频 Top N、图文 Top N 已明确。
- 默认不限制发布时间。只有用户明确要求时才增加时间筛选，且把筛选条件写入交付文件顶部。
- 若用户同时提供本品与竞品，分别完成筛选与理解，禁止把两个对象的排名池混为一个池。

## 数据选择

1. 使用 Spreadsheets Skill 读取底表；Excel 只作为笔记 ID、笔记类型和投放数据来源。
2. 按 SPU name 及用户确认的 nick name 召回分析对象，输出召回命中字段供审计。
3. 按笔记 ID 去重。若同一 ID 有多行，先核验指标口径；不得默默求和。
4. 按视频/图文拆池，再按 `竞价运营消耗` 降序各取 Top N。
5. 生成输入清单，至少包含：对象角色、SPU、命中别名、笔记 ID、笔记类型、消耗排名和全部投放字段。
6. 在获取内容前运行 `scripts/validate_input_manifest.py`。失败则停止。

## 内容来源硬边界

- Excel 中即使含标题、正文、图片或内容描述，也不得用于内容理解、关键句或打标。
- 视频内容只来自 Athena 返回的该笔记标题、正文、封面与真实视频媒体，以及视频理解结果。
- 图文内容只来自 Athena 返回的该笔记标题、正文、封面与按顺序排列的全部图片。
- CTR、CPTI、CPUV、消耗、曝光、点击、互动、I/TI 人群等数据不得作为标签证据。
- Athena 无法核验笔记 ID、媒体缺失或关键内容不可读时，记录真实阻塞；不得用标题或旧标签补齐。

## 视频获取与理解

逐条按笔记 ID 在已登录 Athena 中打开正确笔记：

1. 核验 Athena 笔记 ID 与清单一致；读取作者、发布时间、标题、正文、封面及视频媒体地址。
2. 下载或读取完整视频，不得仅依赖 Athena 正文。
3. 完整观看视频，按时间轴识别开头、转场、产品首次出现、产品讲解段和结尾。
4. 获取完整 ASR 自动转录并保留原始追溯文本；只保守修正明确的品牌词、产品词、专业词、同音词、断句和标点。
5. 对视频花字、字幕、包装文字执行 OCR，并与对应画面时间点绑定。
6. 对产品段同时核对画面、ASR、花字/OCR；一个来源明显错乱时不得用其拼写广告句。
7. 按 `references/content-fields.md` 生成视频内容事实。
8. `一句话总结` 必须概括真实入口、展开结构和产品角色；`关键口播` 必须是围绕目标产品的连续真实原话并带时间戳或帧号。

## 图文获取与理解

逐条按笔记 ID 在已登录 Athena 中打开正确笔记：

1. 核验 Athena 笔记 ID；读取作者、发布时间、标题、正文、封面和全部图片。
2. 按原顺序查看所有图片，不得只看首图、封面或正文。
3. 对每张图片执行 OCR 与视觉复核，记录图片序号、主体、产品露出、花字和关键证据。
4. 合并 Athena 正文与逐图证据，但保持来源分离；不得把模型概述写成原文。
5. 按 `references/content-fields.md` 生成图文内容事实。
6. `一句话总结` 必须概括内容入口、展开方式和产品角色；`关键文案` 必须是 Athena 正文或图片花字中的连续真实原句并注明来源。

## 事实冻结与打标

完成内容事实后才允许打标。投放指标继续保留，但从标签上下文中排除。

For beauty-content labeling, completely read and execute
`components/meihu-labeling-engine/COMPONENT.md`. Treat the component result as
invalid unless its independent validator passes.

必须执行：

1. 运行嵌入式引擎的 `fetch-rules.mjs`，记录 `rules_version`、`rules_hash`、`fetched_at` 和 `source_url`。
2. 锁定本批次规则；同一批次不得混用规则版本。
3. 新鲜获取模式使用已冻结的完整内容事实。
4. 仅重新打标既有过程文件时，不重新读取媒体，严格使用以下证据优先级：
   - 视频：`ASR追溯文本（自动转录） → 一句话总结 → 正文 → 标题`。
   - 图文：`Athena正文 → 图文内容结构 → 一句话总结 → 正文 → 标题`。
5. 逐节点交给嵌入式状态机决定下一步；不得跳步、直接猜最终标签或沿用旧标签。
6. 执行产品实例核验、正向验证、反事实检验、相邻类型排除和独立审计。
7. 每条结果运行嵌入式 `validate-result.mjs`。
8. 全批运行嵌入式 `reconcile-batch.mjs`，确保输入 ID、有效结果和导出行一一对应。

## 字典更新

外部 HTML 是给人阅读和检查的字典外显，不是本 Skill 的执行入口。真正控制打标的是嵌入式 `meihu-labeling-engine` 及其每次获取的完整规则 JSON。

用户说“字典更新了”时：

1. 立即重新获取完整规则，比较版本、哈希、标签定义、工作流步骤和输出契约。
2. 若新规则通过现有结构校验，锁定新版本并对指定范围全量重判；不得只改用户点名样本。
3. 若工作流、字段或验证要求变化导致校验器不兼容，停止打标，先更新本仓库中的引擎组件、父 Skill、校验脚本和测试，再提交版本。
4. HTML 展示页如需同步修改或发布，按其原项目流程完成；不得把修改 HTML 当作引擎已更新的证明。
5. 已开始的批次若规则版本变化，整批重新运行，禁止混合新旧版本。

详细闭环见 `references/dictionary-update-contract.md`。

## 最终交付

只交付一个 `.xlsx`，包含：

- `视频笔记理解`
- `图文笔记理解`

每个 Sheet 保留对应过程字段和全部投放字段，并在末尾只增加三个用户可见字段：

- `打标结果`
- `打标结果原因`
- `打标结果来源`

技术审计轨迹保留在运行目录的 JSON 中，不额外塞入用户可见工作表；工作簿顶部注明规则版本、哈希、获取时间、数据范围和 Top N 口径。

交付字段、公式和格式见 `references/delivery-contract.md`。

## 停止条件

同时满足以下条件才可交付：

- 输入 Top N 行与输出行覆盖完整且笔记 ID 唯一。
- 视频、图文内容均来自 Athena 与真实媒体，不来自 Excel 内容列。
- 所有一句话总结、视频关键口播、图文关键文案均非空。
- 关键口播/文案为真实连续原文、产品相关、中文可读且来源可回溯。
- 每条标签通过嵌入式状态机与结果校验，全批通过 reconciliation。
- 最终两个 Sheet 的 CTR、CPTI、CPUV 公式正确且无公式错误。
- 两个 Sheet 均完成渲染检查，表头、链接、长文本、数字格式和筛选可用。
- `scripts/validate_delivery.py` 返回 0。

任何失败均须回到对应笔记修复；不得将漏处理行改写为“待复核”后交付。

