interface:
  display_name: "美护内容打标引擎"
  short_description: "每次读取完整线上字典并强制逐步执行与审计标签结果"
  default_prompt: "Use $meihu-labeling-engine to classify beauty content with the complete latest rules and an auditable non-skippable workflow."

policy:
  allow_implicit_invocation: true
